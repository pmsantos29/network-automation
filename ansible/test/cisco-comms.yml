- name: Interact with switch
  hosts: linuxVM
  
  tasks:
    - name: Run show running config
      ansible.builtin.command: show run
      register: out

    - name: Display output
      ansible.builtin.debug:
        var: out.stdout_lines